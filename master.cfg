"""
Django's buildmaster config.

All the interesting stuff has been broken out into
individual modules in the djangobotconfig package,
"""

from buildbot.changes.pb import PBChangeSource
from djangobotcfg.slaves import slaves
from djangobotcfg.schedulers import schedulers
from djangobotcfg.builders import builders
from djangobotcfg.status import status

BuildmasterConfig = {
    'slaves': slaves,
    'schedulers': schedulers,
    'builders': builders,
    'status': status,
    'slavePortnum': 9989,
    'change_source': PBChangeSource(),
    'projectName': 'Django',
    'projectURL': 'http://code.djangoproject.com/',
    'buildbotURL': 'http://buildbot.djangoproject.com/',
    'db_url': 'sqlite:///state.sqlite',
}